# API Configuration
# Set to your backend URL to use real APIs; leave empty to use mock data
VITE_API_URL=http://localhost:3000/api/v1

# Set to "true" to force mock data even if API URL is set
VITE_USE_MOCK=false
